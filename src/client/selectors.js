export const appViewSelector = (store) => store.isAppAvailable;

// тут берем данные, которые нужны для отображения конкертно вью, поэтому их не нужно заносить в модель
// например лоадинги, ошибки и т.д.
